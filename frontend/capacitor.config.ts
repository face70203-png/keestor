import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.keestore.app',
  appName: 'KeeStore',
  webDir: 'out',
  bundledWebRuntime: false
};

export default config;
